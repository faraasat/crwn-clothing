version: "3"

services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile.dev
    ports:
      - 3000:3000
    volumes:
      - ./client:/app

  backend:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 5000:5000
